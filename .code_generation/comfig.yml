models:
  - name: source
    prefix: src
    files:
      - yml
  - name: staging
    prefix: stg
    files:
      - sql
      - yml
  - name: curated
    prefix: ods
    files:
      - sql
      - yml
